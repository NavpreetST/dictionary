# Google Gemini API Key (optional - fallback lookup will be used if not provided)
# Get your API key from: https://ai.google.dev/
GEMINI_API_KEY=your_api_key_here

# Google Gemini API Key for Grammar Learning Feature (separate key to distribute load)
# Get your API key from: https://ai.google.dev/
GEMINI_GRAMMAR_API_KEY=your_grammar_api_key_here

# Base URL (for API calls in server components)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Database Configuration
# Environment: 'development' uses SQLite, 'production' uses MongoDB
NODE_ENV=development

# MongoDB Configuration (used when NODE_ENV=production or DATABASE_TYPE=mongodb)
# Replace with your MongoDB connection string
MONGODB_URI=mongodb+srv://navpreetS:Mr_Singh00@cluster0.dveepny.mongodb.net/deutsch-projeckt
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/deutsche-words

# Optional: Force database type (overrides NODE_ENV detection)
# DATABASE_TYPE=sqlite  # or 'mongodb'

# SQLite Database Path (used in development)
SQLITE_PATH=deutsche_words.db
